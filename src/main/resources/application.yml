spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:8080/auth_db
    username: postgres
    password: 4321
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080